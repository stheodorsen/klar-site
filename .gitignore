.DS_Store
.claude/

# Internal design handoff — deliberately NOT published.
# klar-design-brief.md contains business-sensitive material: duty-band
# planning, pending Fødevarestyrelsen certification, pant registration and
# supplier negotiation notes. Keep it in a private repo or out of git.
# site/tools/build-images.py reads its assets/ folder to regenerate imagery.
design_handoff_klar_site/

# The B2B fad spec is internal for the same reason: it names the private
# resale-pricing policy, the pilot's kill criteria and the outbound target
# list. The public repo must never carry it.
klar-b2b-spec.md
